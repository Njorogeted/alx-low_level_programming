#include <stdio.h>
/**
 * main - prints characters using puts
 * puts - prints characters to the output
 * Return: returns 0 when success
 */
int main(void)
{

	const char *jaba = "\"Programming is like building a multilingual puzzle";

	puts(jaba);
	return (0);
}
