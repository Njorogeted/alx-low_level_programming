#include <stdio.h>
/**
 * main - prints characters using prinf
 * printf - prints the characters
 * Return: return 0 when success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
