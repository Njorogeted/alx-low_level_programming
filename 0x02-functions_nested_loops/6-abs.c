#include "main.h"
/**
 * _abs - return the absolute value of an int
 * @x: integer prototype
 * Return: return the absolute number
 */
int _abs(int x)
{
	if (x <= 0)
	{
		return (-x);
	}
	else
	{
		return (x);
	}
}
